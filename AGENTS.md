# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Commits

Never add a `Co-Authored-By` trailer, and never mention Claude, Copilot or any
other assistant in a commit message. The message describes the change, nothing else.
